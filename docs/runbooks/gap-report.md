# Gap report — repo vs. design

**Date:** 2026-08-12 · **Verified against:** [docs/design/software-development-department.md](../design/software-development-department.md) · **Method:** full-repo read + on-paper pipeline walk-through ([example-pbi.md](../onboarding/example-pbi.md))

Overall: the §11 week-1 scope is complete and weeks 2–3 are largely pre-built (all ten agents, four pipeline workflows, digest, onboarding kit). The gaps below are grouped by who they wait on. Items marked **(walk-through)** were found by tracing the example PBI end to end.

---

## A. Waiting on the human

### A1. Open design choices — all decided 2026-08-12 (PR "chore: resolve open choices 1-7")

- [x] **choice-1 — notification channel** = **email**: digest mailed daily (guarded email step in `digest.yml`, activates when the `MAIL_*` secrets are set); critical devops alerts go by email *plus* `@`-mention; the GitHub issue labeled `digest` stays as the always-works fallback. Resolved in `README.md`, `agents/devops.md`, `.github/workflows/digest.yml`.
- [x] **choice-2 — design gate rhythm** = **review as soon as ready** — a design PR waits only for the PO, never for a calendar. First anchored with a `TODO(choice-2)` marker in `workflows/README.md` § How the phases chain (it was marked nowhere), then resolved there.
- [x] **choice-3 — mobile release train** = **Tue/Fri confirmed** — submission days, store review lands 1–2 days later. Dormant until ryd onboards. Resolved in `agents/release-manager.md`.
- [x] **choice-4 — regression core journeys** = **fixed PO-owned list** (initial, opentms-next: login; order → plan → execute → invoice chain; batch invoice run; tenant isolation on list endpoints), automatically extended with every critical-class E2E scenario from approved design docs. Resolved in `agents/test-engineer.md`.
- [x] **choice-5 — monthly budget ceiling** = **€500 hard cap** on the dev-department workspace; digest shows month-to-date consumption (Admin cost-report API, activates when `ANTHROPIC_ADMIN_KEY` is set); revisit after one month of real PBIs. Resolved in `README.md`, `workflows/README.md`, `docs/runbooks/budget.md`, `.github/workflows/digest.yml`.
- [x] **choice-6 — documentation languages** = **the product's ABP localization configuration is the single source of truth**, read at run time (never a hardcoded list); source language EN (ryd) / NL (opentms-next); all other configured languages are generated translations with human spot checks. Resolved in `agents/documentation.md`.
- [x] **choice-7 — performance budgets** = **all default numbers confirmed**, including 99.9% availability; mobile rows defined but dormant until ryd onboards. Resolved in `conventions/performance-budgets.md`.

### A2. Human setup actions (documented, not yet performed)

- [x] Claude Platform workspace `dev-department` + `ANTHROPIC_API_KEY` + spend cap (§2; `docs/runbooks/budget.md`; cap decided: €500). **Done 2026-08-12:** workspace created, spend cap set, API key stored for product onboarding.
- [x] `MAIL_*` secrets for digest email delivery (`MAIL_SMTP_URL`, `MAIL_USERPASS`, `MAIL_FROM`, `MAIL_TO`) — set 2026-08-12; the first four digest runs failed SMTP auth (curl exit 67) during the `MAIL_USERPASS` setup iterations until the `user:password` form was stored. **First delivery confirmed 2026-08-12** — the digest run logs "Digest mailed", receipt confirmed by the PO.
- [ ] ryd onboarding itself: copy `CLAUDE.md` (fill `TODO(fill-from-repo)` stubs), labels, issue form, callers, secrets, `PRODUCT_REPOS` variable (`docs/onboarding/product-onboarding.md`).
- [ ] Branch protection on the product repo's `main` (§8; onboarding step 5).
- [ ] **"Allow GitHub Actions to create and approve pull requests"** on every product repo (Settings → Actions → General → Workflow permissions; onboarding step 4, added 2026-08-13). Discovered 2026-08-13 on opentms-next: with it off, the architect's `gh pr create` was blocked (the design/6 PR had to be opened by hand) and `build.yml`'s draft-PR step fails identically. The setting only lets agents *open* PRs — merging stays impossible for them via branch protection.
- [ ] **Branch protection on `dev-department` itself**: `conventions/README.md` promises "changes only via reviewed PRs", but this repo's `main` is unprotected — this entire build was pushed straight to main. Consistent for bootstrap, inconsistent from the moment the department operates.

---

## B. Genuine gaps (waiting on work)

### B1. Department repo — buildable now

- [x] **Documentation agent is wired into nothing** **(walk-through, most important gap)**: `build.yml` never runs `agents/documentation.md`, yet `agents/reviewer.md` checklist item 11 blocks every user-behavior PR without a docs change (§5, §7). As built, every real feature PR deadlocks on a blocker no agent in the pipeline can resolve. Fix direction: a documentation step in `build.yml` between implementers and reviewer. **Fixed 2026-08-12:** documentation-draft step added to `build.yml` (after tests, before reviewer; reviewer now depends on it) + chain updated in `workflows/README.md` — PR "fix: close gap-report B1 items 1-3".
- [x] **Constitution not referenced by 3 of 10 agents** (§4.0 requires it in *every* agent prompt): `agents/devops.md`, `agents/documentation.md`, `agents/retrospective.md` have no read-the-constitution instruction. (Verified by grep; the other seven have it.) **Fixed 2026-08-12:** "Before anything else — Read `conventions/saas-constitution.md`" sections added to all three; grep now finds ten — PR "fix: close gap-report B1 items 1-3".
- [x] **Test engineer misses the §6 obligation** "performance test at expected volume ×5 for risk class critical/high" — absent from `agents/test-engineer.md` and from the build pipeline. **Fixed 2026-08-12:** ×5 obligation added to the SaaS obligations in `agents/test-engineer.md`, incl. missing-volume-is-a-finding rule — PR "fix: close gap-report B1 items 1-3".
- [ ] **No devops workflow harness** (§6 layer 2): `agents/devops.md` exists, but there is no `repository_dispatch` receiver for alert webhooks and no nightly-audit schedule. Scheduled for §11 week 4+, but currently the agent is unreachable.
- [ ] **No retrospective workflow harness** (§4.9, §5): nothing triggers the agent on `status:released`, and the quarterly pruning run (§9) has no schedule. Scheduled "only after" per §11.
- [ ] **Nothing ever sets `status:released`** (§5): the label exists in the required set, but no workflow flips it (depends on deploy/release workflows — deferred by instruction to onboarding phase 3–4, together with the release-manager harness).
- [ ] **§10 per-domain serialization is not enforced**: build concurrency is per *issue* (`dept-issue-<n>`); two issues touching the same domain can build in parallel. Current mitigation is PO discipline (onboarding phase table). Fix direction: a domain-derived concurrency key or a scheduling check in `build.yml`.
- [ ] **§5 "implementers parallel per domain, own worktree/branch" is simplified**: one backend agent covers all domains sequentially on one branch (documented in `build.yml` header). Acceptable v1 deviation; recorded here so it's a decision, not an accident.
- [ ] **Mobile has no implementer/conventions coverage** **(walk-through)**: `agents/implementer-frontend.md` and `conventions/coding-conventions.md` are web-shaped (shadcn/ui, Core Web Vitals); the mobile budget row (offline-first, sync queue, cold start) has no corresponding build guidance. First mobile-touching PBI will expose this.
- [ ] **Intake re-trigger after answered questions is manual re-label** **(walk-through)**: §5's "Marinus completes → intake again" works only if the label is re-applied (documented in the smoke test + failure comments, but easy to miss; no comment-triggered re-run).
- [ ] **Gate-2 "decline due to design flaw" path** (§5) is described only in a `fix.yml` comment — no runbook step for closing the PR and resetting to `status:design`.
- [x] **Design workflow allowed-tools did not cover the architect's git/PR duties (28 denials, run 31714423931), 2026-08-13**: `design.yml` passed no `--allowedTools`, so the headless run denied every mutating tool call — 28 permission denials in 51 turns (run 31714423931; an earlier read of the log said 25 — the result JSON says 28), no branch, no PR — and the 50-turn cap ran out on ceremony. **Fixed 2026-08-13:** explicit architect allowlist in `design.yml` (write doc, branch/commit/push, open PR, report conflicts); `max-turns` became a caller-configurable input on every pipeline workflow with per-role defaults (intake 25, reviewer 30, architect 80, implementers 60, documentation 40 — the architect produces the largest single artifact in the pipeline); one-write efficiency rule added to `agents/architect.md` — PR "fix: architect capability + per-role turn budgets".
- [x] **Same missing-allowlist defect latent in `build.yml`/`fix.yml` mutating agents**: the implementer, test-engineer and documentation invocations pass no `allowed-tools` either and will deny-loop exactly like the architect the moment phase 3 activates. **Fixed 2026-08-13:** explicit per-agent allowlists on all eight mutating invocations (backend implementer ×3 fix rounds included, frontend implementer, test engineer ×2, documentation in `build.yml`; fix agent in `fix.yml`) — write/edit, git stage/commit/push, and exactly the `gh issue`/`gh pr` commands each definition orders; no branch creation (the workflows check the branch out). Intake and reviewer allowlists verified covering against their definitions in the same pass — PR "fix: build/fix mutating-agent allowlists".
- [x] **Build-phase toolchain grants undecided**: the implementer/test-engineer duties "solution builds and tests pass locally" / "run everything" need product-specific build, test and package-manager commands (dotnet, node/yarn, test runners) that no allowlist grants yet — the agents can now write and push but not compile or run suites. **Fixed 2026-08-13:** caller-configurable `extra-allowed-tools` input on `build.yml`/`fix.yml`, appended to the implementer and test-engineer allowlists only (documentation and reviewer never need a toolchain); opentms-next's callers grant exactly what its CI uses (`dotnet`, `abp`, `npm`, `npx`, `psql`, `./scripts/generate-openapi.sh`); the ryd caller templates carry a fill-before-phase-3 comment — dev-department PR "feat: caller-granted toolchain" + opentms-next PR "chore: grant the build toolchain".
- [x] **Frontend implementer starved at 60 turns on the first platform component (2026-08-14, run 31750544961)**: `error_max_turns` at 61 turns with $2.21 of real progress, commits pushed to `feature/6` and only 5 denials — a budget shortfall, not a tool gap (the backend pass finished in 47). **Fixed 2026-08-14:** `build.yml` implementer budget 60→100 (platform-level frontend components are the largest implementation artifacts; `fix.yml`'s comment-scoped fix rounds stay at 60); the architect's efficiency rule extended to both implementer definitions (large files in one write, builds/tests run sparingly, output read only on failure); re-trigger semantics made explicit in `build.yml` (fresh clone + `checkout -B` to the pushed tip — continue from a defined state, never reset to main; header documents it); `run-agent` now uploads every agent's execution output as a workflow artifact (30 days) — this run's 5 denied commands were unrecoverable (transcript hidden, no artifact), so no allowlist was extended blind; the next denial list is auditable — PR "fix: implementer budget 100 + diagnosable runs".
- [x] **Frontend implementer burned 100 turns routing around a stale contract (2026-08-14, run 31752956967)**: the branch's OpenAPI spec lacked the grid endpoints the backend had implemented — no duty or workflow step regenerates spec + typed client after backend work; CI only *checks* freshness at PR time — so the agent hand-patched the spec via a patch script (a hard-rule violation its definition forbids), spent over half the run grepping `node_modules` type definitions shape by shape, wrote all four components in the final tenth, and died at the cap with zero commits. First incident diagnosed from the run-agent execution artifact (PR #16 — it works). **Fixed 2026-08-14:** mechanical contract-refresh step in `build.yml` between the implementers (new caller-owned `contract-refresh-command` input; changes committed and pushed with zero agent turns; opentms-next wires it to `scripts/contract-refresh-ci.sh` — disposable Postgres, DbMigrator, spec, client); frontend hard rule now names the anti-pattern (missing endpoint = blocked report, never a spec patch); both implementer definitions get batched API discovery and commit checkpoints (the cap must cost one unit, never the run). Budget deliberately stays 100 — the real work fit in the run once the detours are gone — PR "fix: contract refresh + implementer checkpoints".
- [x] **Contract-refresh script invisible to the pre-existing feature branch (2026-08-14, run 31775362672)**: the refresh step runs on `feature/<issue>`, but that branch was cut from main before `scripts/contract-refresh-ci.sh` merged — and nothing ever folded main forward into an in-flight branch, so no main-side tooling or design fix could reach it (exit 127, file not found). **Fixed 2026-08-14:** the setup step forward-merges `origin/<default>` into the feature branch on every run and pushes the result — a merge commit, never a rewrite, so §5's history rule holds; a conflict fails the run loudly for a human to resolve — PR "fix: forward-merge main into in-flight feature branches".
- [x] **Test engineer starved at 60 turns standing up the product's first test infrastructure (2026-08-14, run 31776975497)**: died mid-run at "now run the integration tests" — both backend test files written, 13 validation tests green, and the frontend's entire component-test stack (vitest/happy-dom/testing-library — the scaffold had none) stood up in the final quarter; roughly a third of the run lost to an `ISettingManager` API-discovery rabbit hole (source greps, nuget spelunking, throwaway inspection project); zero commits, so all of it evaporated with the runner. Same three causes as the implementer incidents — first-run infrastructure tax, unbatched API discovery, no checkpoints — because the efficiency/checkpoint rules were scoped to the architect and implementers only. **Fixed 2026-08-14:** test-engineer budget 60→100 (the first run on a product also builds its test infrastructure); efficiency + commit-checkpoint rules added to `test-engineer.md` and `documentation.md` (the last two mutating definitions without them). Recovery for the already-merged #6: seed commit on the preserved `feature/6` so the pipeline can open a fresh draft PR and deliver the missing test coverage and manual page through the normal test → docs → review chain — PR "fix: test-engineer budget + checkpoints".
- [x] **Gate-1 design remarks reached no agent (2026-08-14, run 31795608586 skipped)**: the fix loop deliberately serves `Feature:` PRs only, and the design workflow had no revision mode — a submitted review with remarks on an open `Design:` PR fired `dept-fix`, matched nothing, and died in a skip; every design iteration was silently manual. **Fixed 2026-08-14:** third `design-fix` mode in `design.yml` mirroring the gate-2 loop — caller triggers on submitted non-approval reviews on `Design:` PRs; the architect gets a "Revision runs" section (read every remark, revise the doc on the same branch in one write, reply only where a remark rests on a misunderstanding, never force-push, never a new PR) and a revision allowlist without branch/PR creation; approval remains the human merge. Callers updated (opentms-next + ryd template) — PR "feat: gate-1 design fix loop".
- [x] **Action failed a successful agent run post-hoc on a turn-count check (2026-08-14, run 31797494981)**: the test engineer finished its assignment — suite committed and pushed, `RESULT: findings:4` posted, result `subtype: success` — in 128 turns; the runtime never enforced `maxTurns: 100` during execution (verified: no subagents, genuinely 128 main-loop turns), and `claude-code-action` then failed the completed step for exceeding the cap, killing the pipeline after the budget was already spent and doubling the recovery cost. **Fixed 2026-08-14:** `run-agent` now treats the execution file as the contract — result `subtype: success` plus the mandatory RESULT line decide the step outcome; a genuine `error_max_turns` still fails; a successful overshoot passes with a loud `::warning` and step-summary budget signal (§8 stays a signal, not a post-hoc punishment) — PR "fix: run-agent outcome contract".
- [x] **Architect revision half-applied a gate-1 rename (2026-08-14, design PR opentms-next#11)**: the remark renamed `IAdministrationUserAppService`/`IAdministrationRoleAppService`; the revision run renamed exactly those two interfaces and left the implementing classes (`AdministrationUserAppService`, `AdministrationRoleAppService`) untouched in the domain impact, migration steps and risk analysis — a half-applied remark that reads as addressed. **Fixed 2026-08-14:** completing rename pushed by hand at gate 1 (9 occurrences); `architect.md` revision rule now orders applying each remark to its full reach — every occurrence, every convention-paired counterpart — and searching the doc for old names before committing — PR "fix: architect revision completeness".
- [x] **Inline review comments structurally invisible to the fix loops (2026-08-17, design PR opentms-next#11)**: the PO's remark ("change POST /users/grid to GET") was left twice as an inline file/line comment and never read by any revision run — agent allowlists expose `gh pr view`, which returns review bodies and top-level comments only; inline threads need `gh api`, which agents don't get. The architect itself reported the blindness on the PR and asked the PO to copy remarks into top-level comments. The gate-2 loop (`fix.yml`) had the identical latent gap — human feature-PR remarks are typically inline. **Fixed 2026-08-17:** both loops now run a mechanical "collect review threads" step (workflow-side `gh api`, no new agent permissions) that writes all review bodies + inline comments with file/line context to the git-excluded `.dev-department/review-threads.md`; assignments and `architect.md` point the agents there first — PR "fix: collect review threads for the fix loops".
- [ ] **Build job provides no test infrastructure**: `build.yml`'s build job has no Postgres service container and no setup-dotnet/setup-node steps (the product CI has all three) — so even with the toolchain granted, the integration/migration/idempotency tests the risk matrix mandates have no database to hit, and the runner's preinstalled SDKs may not match the product's versions. Provision the job environment at phase-3 activation, mirroring the product CI's services and setup steps. **Partially closed 2026-08-14 as a side effect of the contract-refresh step:** opentms-next's refresh script leaves its disposable Postgres running with a migrated host database for the rest of the job, so the test engineer has a database when the step ran; SDK version pinning still relies on the runner image, and a run whose contract step is skipped still has no database.

### B2. Product-side — scheduled to arrive with onboarding phases (§11), not slipped

- [ ] Product CI: build + regression on merge; migration test against a production-like schema *with data* (§4.5).
- [ ] k6 nightly load tests per budget row; Lighthouse/bundle budgets in CI (`conventions/performance-budgets.md` enforcement).
- [ ] Deploy/release workflows: Cloud Run traffic splitting + auto-rollback, contract calendar tooling, mobile release train (§4.7 — agent exists, harness deliberately deferred).
- [ ] GCP layer 1 (§6): structured logging → Cloud Logging, Error Reporting, uptime checks/synthetic journeys, SLOs + burn-rate alerting, Cloud Run probes; alert webhook → `repository_dispatch`.
- [ ] Error-budget tracking that actually feeds the release manager's freeze rule (currently prose without a data source).
- [ ] Docs infrastructure (§7): docs site skeleton, demo tenant + seed data, screenshot workflow, OpenAPI publication + breaking-change detection in CI, architecture overview generation, engineering changelog.
- [ ] GitHub Environments with per-environment secrets (§8) — at deploy phase.
- [ ] OpenTMS-next domain map lands in that repo's CLAUDE.md at its onboarding (§3.1 — content already in the design doc).

### B3. Observability of the department itself

- [ ] Digest budget line is a manual pointer to the console — no automated spend reporting (acceptable until choice-5; noted for completeness). **Status 2026-08-12: implemented, deliberately inactive (individual org).** The automation shipped in PR 2 (Admin cost-report fetch, guarded on `ANTHROPIC_ADMIN_KEY`) but stays dormant by decision: Admin API keys require a *team* organization and this Console account is an *individual* one — converting it for one convenience line is not worth it. The workspace spend cap is the actual guardrail; the digest shows the cap plus a setup pointer. Activates by setting `ANTHROPIC_ADMIN_KEY` if the org ever converts to team.
- [ ] Per-agent API attribution needs key-per-workflow split (documented as refinement in `docs/runbooks/budget.md`, not done).

---

**Reading order for fixing:** B1 items 1–3 before the first real PBI runs (the docs deadlock guarantees a failed first feature; the missing constitution reads and the ×5 perf test are two-line agent edits); A2 branch protection on this repo at the same moment; everything else follows the §11 phase it belongs to.
