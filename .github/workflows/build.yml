# Reusable: build phase (§5). Caller triggers on issues labeled status:build.
# Sequential pipeline on one feature branch: implementer-backend → contract refresh
# (mechanical, caller-owned command — the frontend must never inherit a stale
# spec/client) → implementer-frontend
# → draft feature PR → test engineer → documentation draft → reviewer, with at most ONE internal fix round
# after test findings and ONE after reviewer blockers (§4.6: blockers go back to the
# implementer before the PR reaches gate 2). The PR leaves draft state only at zero
# blockers.
# Convergence (PO decision 2026-08-17, supersedes the single-fix-round reading of
# §4.6): once gate 1 approves a design, the pipeline drives itself to green. A
# mechanical verify gate (caller-owned command) checks the tree compiles after
# the implementers, with its own fix round; and any run that ends without a
# gate-2-ready PR — findings left, blockers left, or a failed step — dispatches
# the NEXT ROUND of itself via workflow_dispatch (the one event GITHUB_TOKEN may
# self-fire), bounded by max-rounds. At the cap it pauses loudly and a human
# decides; AGENTS_ENABLED remains the kill switch for the whole loop.
# Re-trigger semantics: every run starts from a fresh clone, and an existing
# feature/<issue> branch is force-aligned to its pushed tip (checkout -B) — a
# defined state with no local leftovers — then main is forward-merged in (a
# merge commit, never a rewrite: main-side design fixes and pipeline tooling
# must reach in-flight branches; conflicts fail loudly for a human) and the
# pipeline continues on it: partial work and review history are preserved on
# purpose (§5), never reset to main. A restart from scratch is a human act:
# delete the branch, then re-apply status:build.
name: dept-build

on:
  workflow_call:
    inputs:
      model:
        description: Model for every agent run in this workflow — explicit and workflow-owned; checked-in settings files never decide it
        required: false
        type: string
        default: claude-sonnet-4-6
      # Per-role turn budgets (§8) — caller-configurable, like the model input.
      # Headroom policy (PO decision 2026-08-18): caps are 2-3x observed
      # successful-run turn counts and catch pathology, not performance — a run
      # killed at the cap costs its full spend plus the convergence round that
      # re-does it. The real brakes are max-rounds, the workspace spend cap and
      # the kill switch. The retrospective after ~5 shipped features tightens
      # these from the run artifacts' num_turns/cost data.
      max-turns-implementer:
        # Largest implementation artifacts observed: platform-level frontend components (runs 31750544961, 31752956967).
        required: false
        type: number
        default: 150
      max-turns-test-engineer:
        # The first run on a product also stands up its test infrastructure (run 31776975497).
        required: false
        type: number
        default: 150
      max-turns-documentation:
        required: false
        type: number
        default: 80
      max-turns-reviewer:
        # Full-checklist review of a multi-thousand-line diff (starved at 30, 2026-08-18).
        required: false
        type: number
        default: 80
      extra-allowed-tools:
        description: >-
          Product toolchain commands appended to the implementer and
          test-engineer allowlists (build, test, package manager — e.g.
          "Bash(dotnet:*),Bash(npm:*)"). The lists below cover the git/gh duties
          only; the toolchain is product-specific, so the caller grants it (gap
          report B1). Documentation and reviewer runs never get it.
        required: false
        type: string
        default: ""
      ci-workflow:
        description: >-
          Name of the product's CI workflow to dispatch on the feature branch
          when the PR goes gate-2-ready. Agent pushes use GITHUB_TOKEN, whose
          events never trigger workflows — without this kick the PR shows no
          checks on agent-pushed heads. The workflow must carry a
          workflow_dispatch trigger. Empty = skip.
        required: false
        type: string
        default: ""
      verify-command:
        description: >-
          Product command that proves the tree compiles and builds (compile +
          lint, NOT the test suite — tests are the test engineer's). Runs
          mechanically after the implementers; a red result triggers a
          compile-fix agent round, then re-verifies. Empty = skip.
        required: false
        type: string
        default: ""
      round:
        description: Internal convergence counter — set by the pipeline's own re-dispatch, leave at 1 for manual runs
        required: false
        type: number
        default: 1
      max-rounds:
        description: >-
          Hard cap on self-dispatched convergence rounds (§8 — the loop must
          have a brake besides the kill switch). At the cap the pipeline pauses
          loudly instead of dispatching again.
        required: false
        type: number
        default: 3
      contract-refresh-command:
        description: >-
          Product command run mechanically between the backend and frontend
          implementers to regenerate the API contract artifacts (OpenAPI spec +
          typed client) — typically a repo script that boots the product's
          dependencies and regenerates both. Resulting changes are committed and
          pushed without spending agent turns (incident 2026-08-14, run
          31752956967: the frontend inherited a stale spec and hand-patched it,
          burning the run). Empty = skip.
        required: false
        type: string
        default: ""
      issue-number:
        description: PBI issue number labeled status:build (design PR must be merged)
        required: true
        type: number
    secrets:
      ANTHROPIC_API_KEY:
        required: true
      DEPT_SYNC_TOKEN:
        required: false

concurrency:
  group: dept-issue-${{ inputs.issue-number }}
  cancel-in-progress: false

permissions:
  contents: write
  issues: write
  pull-requests: write
  # For the convergence loop's self-dispatch (workflow_dispatch re-entry).
  actions: write

jobs:
  gate:
    name: Kill switch (AGENTS_ENABLED, §8)
    runs-on: ubuntu-latest
    outputs:
      enabled: ${{ steps.check.outputs.enabled }}
    steps:
      - id: check
        run: |
          if [ "${{ vars.AGENTS_ENABLED }}" = "false" ]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "AGENTS_ENABLED=false — kill switch active, exiting cleanly."
          else
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          fi

  build:
    name: Implementers → tests → review
    needs: gate
    if: needs.gate.outputs.enabled == 'true'
    runs-on: ubuntu-latest
    env:
      # Comma-prefixed so it appends cleanly to the implementer/test-engineer
      # allowlists below; empty when the caller grants no toolchain.
      EXTRA_TOOLS: ${{ inputs.extra-allowed-tools != '' && format(',{0}', inputs.extra-allowed-tools) || '' }}
    steps:
      - name: Check out product repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync department (fresh from main, §3)
        uses: actions/checkout@v4
        with:
          repository: marinusbrink/dev-department
          ref: main
          path: .dev-department
          token: ${{ secrets.DEPT_SYNC_TOKEN || github.token }}

      - name: Locate design doc and prepare feature branch
        id: setup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          echo ".dev-department/" >> .git/info/exclude
          ISSUE="${{ inputs.issue-number }}"
          DESIGN=$(ls docs/designs/${ISSUE}-*.md 2>/dev/null | head -1)
          if [ -z "$DESIGN" ]; then
            echo "::error::No approved design doc docs/designs/${ISSUE}-*.md on main — did this issue pass gate 1?"
            exit 1
          fi
          BRANCH="feature/${ISSUE}"
          DEFAULT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          if git fetch origin "$BRANCH" 2>/dev/null; then
            # Defined state, not a guess: exactly the pushed tip (header: re-trigger semantics).
            git checkout -B "$BRANCH" "origin/$BRANCH"
            # Fold main forward into the in-flight branch: design fixes and
            # pipeline tooling merged after the branch was cut must reach it
            # (incident 2026-08-14, run 31775362672: the contract-refresh script
            # on main was invisible to feature/6). A merge, never a rewrite —
            # §5's history rule holds; a conflict fails loudly for a human.
            if ! git merge --no-edit "origin/$DEFAULT_BRANCH"; then
              echo "::error::Merging origin/$DEFAULT_BRANCH into $BRANCH conflicts — resolve and push by hand, then re-apply status:build."
              exit 1
            fi
            git push origin "$BRANCH"
          else
            git checkout -b "$BRANCH"
            git push -u origin "$BRANCH"
          fi
          echo "design=$DESIGN" >> "$GITHUB_OUTPUT"
          echo "branch=$BRANCH" >> "$GITHUB_OUTPUT"

      - name: Backend implementer
        id: be
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/implementer-backend.md
          max-turns: ${{ inputs.max-turns-implementer }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          # Explicit allowlist — same failure class as the architect incident
          # 2026-08-13 (run 31714423931: no allowlist passed, headless runs deny
          # every mutating tool call, 28 denials burned the whole turn budget).
          # Exactly the tools this agent's duties name: write code, stage/commit/
          # push the assigned branch, read and comment the PBI and draft PR. No
          # branch creation — the workflow checks the branch out. Build/test/
          # package-manager commands come from the caller's extra-allowed-tools
          # input — product-specific, never hardcoded here (gap report B1).
          allowed-tools: "Read,Grep,Glob,Edit,Write,Bash(git status:*),Bash(git add:*),Bash(git commit:*),Bash(git push:*),Bash(git diff:*),Bash(git log:*),Bash(gh issue view:*),Bash(gh issue comment:*),Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr comment:*)${{ env.EXTRA_TOOLS }}"
          assignment: |
            Repository: ${{ github.repository }} — issue #${{ inputs.issue-number }}
            Approved design doc: ${{ steps.setup.outputs.design }}
            Branch (already checked out): ${{ steps.setup.outputs.branch }}
            Implement the backend work for every domain in the design's Domain impact
            section, one domain at a time with a commit per domain, honoring your
            domain-boundary rules within each. If a draft feature PR for this branch
            has unresolved blocker/finding comments, addressing them is part of this
            assignment. If the design assigns no backend work, do nothing and end
            with RESULT: done. Push your commits to the branch.

      - name: Contract refresh (mechanical — zero agent turns)
        if: steps.be.outputs.result == 'done' && inputs.contract-refresh-command != ''
        run: |
          ${{ inputs.contract-refresh-command }}
          if [ -n "$(git status --porcelain)" ]; then
            git add -A
            git commit -m "chore: contract refresh — regenerated OpenAPI spec and typed client (#${{ inputs.issue-number }})"
            git push origin "${{ steps.setup.outputs.branch }}"
          fi

      - name: Frontend implementer
        id: fe
        if: steps.be.outputs.result == 'done'
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/implementer-frontend.md
          max-turns: ${{ inputs.max-turns-implementer }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          # Same mutating-agent allowlist — rationale at the backend implementer step.
          allowed-tools: "Read,Grep,Glob,Edit,Write,Bash(git status:*),Bash(git add:*),Bash(git commit:*),Bash(git push:*),Bash(git diff:*),Bash(git log:*),Bash(gh issue view:*),Bash(gh issue comment:*),Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr comment:*)${{ env.EXTRA_TOOLS }}"
          assignment: |
            Repository: ${{ github.repository }} — issue #${{ inputs.issue-number }}
            Approved design doc: ${{ steps.setup.outputs.design }}
            Branch (already checked out): ${{ steps.setup.outputs.branch }}
            Implement the UI work per the design's UI design section. If a draft
            feature PR for this branch has unresolved blocker/finding comments in
            your area, addressing them is part of this assignment. If the design
            assigns no frontend work, do nothing and end with RESULT: done.
            Push your commits to the branch.

      - name: Verify the tree compiles (mechanical — zero agent turns)
        id: verify
        if: steps.fe.outputs.result == 'done' && inputs.verify-command != ''
        continue-on-error: true
        run: ${{ inputs.verify-command }}

      - name: Compile-fix round
        id: fixcompile
        if: steps.verify.outcome == 'failure'
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/implementer-backend.md
          max-turns: ${{ inputs.max-turns-implementer }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          # Same mutating-agent allowlist — rationale at the backend implementer step.
          allowed-tools: "Read,Grep,Glob,Edit,Write,Bash(git status:*),Bash(git add:*),Bash(git commit:*),Bash(git push:*),Bash(git diff:*),Bash(git log:*),Bash(gh issue view:*),Bash(gh issue comment:*),Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr comment:*)${{ env.EXTRA_TOOLS }}"
          assignment: |
            Repository: ${{ github.repository }} — issue #${{ inputs.issue-number }}, branch ${{ steps.setup.outputs.branch }} (already checked out)
            The mechanical verification failed: the tree does not compile/lint/build.
            Verification command: ${{ inputs.verify-command }}
            Run it, read the failures, and fix the application code until it is green
            (frontend fixes follow .dev-department/agents/implementer-frontend.md
            discipline; never weaken or delete tests to get green). Push your fix
            commits. End with RESULT: done or RESULT: blocked.

      - name: Verify again after the compile-fix round (loud — a red tree stops this round)
        if: steps.verify.outcome == 'failure'
        run: ${{ inputs.verify-command }}

      - name: Open draft feature PR
        id: draftpr
        if: steps.fe.outputs.result == 'done'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          BRANCH="${{ steps.setup.outputs.branch }}"
          DESIGN="${{ steps.setup.outputs.design }}"
          ISSUE="${{ inputs.issue-number }}"
          git push origin "$BRANCH" || true
          PR=$(gh pr list --repo "${{ github.repository }}" --head "$BRANCH" --state open --json number -q '.[0].number' || true)
          if [ -z "$PR" ]; then
            FEATURE=$(head -1 "$DESIGN" | sed -e 's/^# *//' -e 's/^Design: *//')
            PR=$(gh pr create --repo "${{ github.repository }}" --draft --head "$BRANCH" \
              --title "Feature: ${FEATURE} (#${ISSUE})" \
              --body "Implements #${ISSUE} per \`${DESIGN}\`. Draft until the reviewer agent reports zero blockers (§4.6)." \
              | grep -oE '[0-9]+$')
          fi
          echo "pr=$PR" >> "$GITHUB_OUTPUT"

      - name: Test engineer
        id: te
        if: steps.fe.outputs.result == 'done'
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/test-engineer.md
          max-turns: ${{ inputs.max-turns-test-engineer }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          # Same mutating-agent allowlist — the test engineer writes, commits and
          # pushes test code and posts findings; rationale at the backend implementer step.
          allowed-tools: "Read,Grep,Glob,Edit,Write,Bash(git status:*),Bash(git add:*),Bash(git commit:*),Bash(git push:*),Bash(git diff:*),Bash(git log:*),Bash(gh issue view:*),Bash(gh issue comment:*),Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr comment:*)${{ env.EXTRA_TOOLS }}"
          assignment: |
            Repository: ${{ github.repository }} — issue #${{ inputs.issue-number }}
            Approved design doc: ${{ steps.setup.outputs.design }}
            Branch (already checked out): ${{ steps.setup.outputs.branch }} — draft PR #${{ steps.draftpr.outputs.pr }}
            Implement and run the test work per the design's test risk analysis.
            Post bug findings on the draft PR. Push your test commits to the branch.

      - name: Fix round after test findings
        id: fixtests
        if: startsWith(steps.te.outputs.result, 'findings:')
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/implementer-backend.md
          max-turns: ${{ inputs.max-turns-implementer }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          # Same mutating-agent allowlist — rationale at the backend implementer step.
          allowed-tools: "Read,Grep,Glob,Edit,Write,Bash(git status:*),Bash(git add:*),Bash(git commit:*),Bash(git push:*),Bash(git diff:*),Bash(git log:*),Bash(gh issue view:*),Bash(gh issue comment:*),Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr comment:*)${{ env.EXTRA_TOOLS }}"
          assignment: |
            Repository: ${{ github.repository }} — issue #${{ inputs.issue-number }}, branch ${{ steps.setup.outputs.branch }}
            The test engineer posted bug findings on draft PR #${{ steps.draftpr.outputs.pr }}.
            Address every finding by fixing the application code (frontend fixes follow
            .dev-department/agents/implementer-frontend.md discipline). Never edit or
            weaken test files. Push fix commits. End with RESULT: done or RESULT: blocked.

      - name: Test engineer — verify fixes
        id: te2
        if: steps.fixtests.outputs.result == 'done'
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/test-engineer.md
          max-turns: ${{ inputs.max-turns-test-engineer }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          # Same mutating-agent allowlist — rationale at the backend implementer step.
          allowed-tools: "Read,Grep,Glob,Edit,Write,Bash(git status:*),Bash(git add:*),Bash(git commit:*),Bash(git push:*),Bash(git diff:*),Bash(git log:*),Bash(gh issue view:*),Bash(gh issue comment:*),Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr comment:*)${{ env.EXTRA_TOOLS }}"
          assignment: |
            Repository: ${{ github.repository }} — issue #${{ inputs.issue-number }}, branch ${{ steps.setup.outputs.branch }}, draft PR #${{ steps.draftpr.outputs.pr }}
            Re-run: verify your earlier findings are resolved and the suite is green.

      - name: Documentation draft (§4.10, §7.1 — the feature PR must contain its docs change)
        id: docs
        if: steps.te.outputs.result == 'passed' || steps.te2.outputs.result == 'passed'
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/documentation.md
          max-turns: ${{ inputs.max-turns-documentation }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          # Mutating-agent allowlist minus the PR-diff read: the docs agent writes
          # the manual page from PBI + design doc (its sources of truth), commits,
          # pushes, comments. Rationale at the backend implementer step.
          allowed-tools: "Read,Grep,Glob,Edit,Write,Bash(git status:*),Bash(git add:*),Bash(git commit:*),Bash(git push:*),Bash(git diff:*),Bash(git log:*),Bash(gh issue view:*),Bash(gh issue comment:*),Bash(gh pr view:*),Bash(gh pr comment:*)"
          assignment: |
            Run mode: draft.
            Repository: ${{ github.repository }} — issue #${{ inputs.issue-number }}
            Approved design doc: ${{ steps.setup.outputs.design }}
            Branch (already checked out): ${{ steps.setup.outputs.branch }} — draft PR #${{ steps.draftpr.outputs.pr }}
            From the PBI and the design doc, write the draft manual page for this
            feature in docs/manual/ (user language, placeholder images for the
            screenshot workflow), commit and push it to the branch, so the feature
            PR contains the docs change the reviewer requires (§5). If the design
            changes no user-visible behavior, post a one-line comment on the draft
            PR saying so instead of writing a page, and still end with RESULT: drafted.

      - name: Reviewer agent
        id: review
        if: steps.docs.outputs.result == 'drafted'
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/reviewer.md
          max-turns: ${{ inputs.max-turns-reviewer }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          allowed-tools: "Read,Grep,Glob,Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr review:*),Bash(gh pr edit:*),Bash(gh pr comment:*),Bash(gh issue view:*)"
          assignment: |
            Repository: ${{ github.repository }}
            Review draft PR #${{ steps.draftpr.outputs.pr }} (issue #${{ inputs.issue-number }},
            design doc ${{ steps.setup.outputs.design }}).

      - name: Fix round after reviewer blockers
        id: fixblockers
        if: startsWith(steps.review.outputs.result, 'blockers:')
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/implementer-backend.md
          max-turns: ${{ inputs.max-turns-implementer }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          # Same mutating-agent allowlist — rationale at the backend implementer step.
          allowed-tools: "Read,Grep,Glob,Edit,Write,Bash(git status:*),Bash(git add:*),Bash(git commit:*),Bash(git push:*),Bash(git diff:*),Bash(git log:*),Bash(gh issue view:*),Bash(gh issue comment:*),Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr comment:*)${{ env.EXTRA_TOOLS }}"
          assignment: |
            Repository: ${{ github.repository }} — issue #${{ inputs.issue-number }}, branch ${{ steps.setup.outputs.branch }}
            The reviewer posted [blocker] findings on draft PR #${{ steps.draftpr.outputs.pr }}.
            Address every blocker (frontend fixes follow .dev-department/agents/implementer-frontend.md
            discipline; a blocker you believe is wrong is a comment reply, never a silent skip).
            Push fix commits. End with RESULT: done or RESULT: blocked.

      - name: Reviewer agent — re-review
        id: review2
        if: steps.fixblockers.outputs.result == 'done'
        uses: ./.dev-department/.github/actions/run-agent
        with:
          model: ${{ inputs.model }}
          agent-file: agents/reviewer.md
          max-turns: ${{ inputs.max-turns-reviewer }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          allowed-tools: "Read,Grep,Glob,Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr review:*),Bash(gh pr edit:*),Bash(gh pr comment:*),Bash(gh issue view:*)"
          assignment: |
            Repository: ${{ github.repository }}
            Re-review draft PR #${{ steps.draftpr.outputs.pr }} (issue #${{ inputs.issue-number }},
            design doc ${{ steps.setup.outputs.design }}) after the blocker fix round.

      - name: Zero blockers → feature PR ready for gate 2
        if: steps.review.outputs.result == 'approved' || steps.review2.outputs.result == 'approved'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh pr ready "${{ steps.draftpr.outputs.pr }}" --repo "${{ github.repository }}"
          # Agent pushes (GITHUB_TOKEN) never trigger CI — dispatch it on the
          # branch so the gate-2 head carries real checks. workflow_dispatch is
          # exempt from the token-trigger suppression; check runs attach to the
          # head commit and show on the PR. Deliberately NOT a PAT: agents
          # acting as a human actor would defeat the [bot] guards that keep
          # agent events from re-triggering agent workflows.
          if [ -n "${{ inputs.ci-workflow }}" ]; then
            gh workflow run "${{ inputs.ci-workflow }}" --repo "${{ github.repository }}" --ref "${{ steps.setup.outputs.branch }}"
            CINOTE=" CI dispatched on \`${{ steps.setup.outputs.branch }}\` — verify the checks are green on the PR before merging."
          else
            CINOTE=""
          fi
          gh issue comment "${{ inputs.issue-number }}" --repo "${{ github.repository }}" --body \
            "✅ Feature PR #${{ steps.draftpr.outputs.pr }} passed internal review with zero blockers and awaits gate 2.${CINOTE}"

      - name: Not green yet → dispatch the next convergence round
        if: success() && steps.review.outputs.result != 'approved' && steps.review2.outputs.result != 'approved' && inputs.round < inputs.max-rounds
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          NEXT=$(( ${{ inputs.round }} + 1 ))
          gh workflow run "${{ github.workflow }}" --repo "${{ github.repository }}" \
            -f issue-number="${{ inputs.issue-number }}" -f round="$NEXT"
          gh issue comment "${{ inputs.issue-number }}" --repo "${{ github.repository }}" --body \
            "🔁 Build round ${{ inputs.round }} ended without a gate-2-ready PR — dispatching round $NEXT of ${{ inputs.max-rounds }} (convergence loop, PO decision 2026-08-17).
          Phase results: backend=\`${{ steps.be.outputs.result }}\` frontend=\`${{ steps.fe.outputs.result }}\` tests=\`${{ steps.te.outputs.result }}\` tests-after-fix=\`${{ steps.te2.outputs.result }}\` docs=\`${{ steps.docs.outputs.result }}\` review=\`${{ steps.review.outputs.result }}\` review-after-fix=\`${{ steps.review2.outputs.result }}\`."

      - name: Round cap reached — pause loudly (no silent stops)
        if: success() && steps.review.outputs.result != 'approved' && steps.review2.outputs.result != 'approved' && inputs.round >= inputs.max-rounds
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue comment "${{ inputs.issue-number }}" --repo "${{ github.repository }}" --body \
            "⏸️ Build pipeline paused: ${{ inputs.max-rounds }} convergence rounds spent without a gate-2-ready PR (issue #${{ inputs.issue-number }}).
          Phase results: backend=\`${{ steps.be.outputs.result }}\` frontend=\`${{ steps.fe.outputs.result }}\` tests=\`${{ steps.te.outputs.result }}\` tests-after-fix=\`${{ steps.te2.outputs.result }}\` docs=\`${{ steps.docs.outputs.result }}\` review=\`${{ steps.review.outputs.result }}\` review-after-fix=\`${{ steps.review2.outputs.result }}\`.
          The responsible agent reported details in comments here or on the draft PR. This cap is the loop's brake (§8): investigate, then re-apply \`status:build\` for a fresh round set on the same branch."

  report-failure:
    name: Failure is visible (§1, §8)
    needs: [gate, build]
    if: always() && needs.gate.outputs.enabled == 'true' && contains(needs.*.result, 'failure')
    runs-on: ubuntu-latest
    steps:
      - env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ inputs.round < inputs.max-rounds }}" = "true" ]; then
            NEXT=$(( ${{ inputs.round }} + 1 ))
            gh workflow run "${{ github.workflow }}" --repo "${{ github.repository }}" \
              -f issue-number="${{ inputs.issue-number }}" -f round="$NEXT"
            gh issue comment "${{ inputs.issue-number }}" --repo "${{ github.repository }}" --body \
              "⚠️ **Agent workflow \`build\` failed** — [run log](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}). 🔁 Dispatching convergence round $NEXT of ${{ inputs.max-rounds }} automatically (the feature branch and its checkpoints are preserved; agents continue from the pushed tip)."
          else
            gh issue comment "${{ inputs.issue-number }}" --repo "${{ github.repository }}" --body \
              "⚠️ **Agent workflow \`build\` failed** — [run log](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}). Silent failure is forbidden (design §8); this comment is the alarm. The ${{ inputs.max-rounds }}-round convergence cap is spent: investigate, then re-apply \`status:build\` to start a fresh round set (the feature branch is preserved)."
          fi
